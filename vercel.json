{
  "buildCommand": "echo 'Using pre-built files'",
  "outputDirectory": "build",
  "installCommand": "echo 'skip install'",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
